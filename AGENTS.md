# 프로젝트 작업 지침

## 목적과 코드 구조

One UI와 실제 앱 화면을 유지하면서 물리적인 접힘·펼침 순간에만 시스템 블러를
적용한다. 현재 검증 기기는 Galaxy Z Fold7 SM-F966N / Android 16이다.

- `system/`: ADB 시험과 Shizuku 앱이 공유하는 compositor 엔진 및 상태 머신.
- `app/`: Shizuku 연결, 설정 UI, 저장된 실행 상태 복구.
- `tools/fold-system.py`: 앱 설치 없는 임시 실행·중지·진단 도구.
- `legacy/`: 빌드에서 제외된 MediaProjection/Accessibility 초기 실험.
- 최신 사용법은 `README.md`, 앱 구조는 `docs/SHIZUKU_APP.md`를 확인한다.
  `PROGRESS.md`와 `docs/SYSTEM_BACKEND.md`의 과거 기록은 현재 코드와 다를 수 있다.

## 사용자에게 실행 방법 안내하기

사용자가 체험·데모·잠깐 실행을 원하면 **APK와 Shizuku 설치 없이 가능한 ADB
임시 실행 방법을 먼저 안내**한다. 설치를 필수 조건처럼 설명하지 않는다.
상시 사용을 원하면 Shizuku 앱 방식을 안내하되, 재부팅 복구 조건을 구분한다.
사용자가 이미 방식을 선택했거나 실행을 요청했다면 같은 확인을 반복하지 않는다.
문서 작성 요청만 받은 경우 실제 설치·실행·재부팅까지 임의로 진행하지 않는다.

| 방식 | 기기에 앱 설치 | 수명 / 복구 |
| --- | --- | --- |
| ADB 임시 체험 | Fold Transition과 Shizuku 모두 불필요 | 기본 10분, 자동 종료, 부팅 자동 실행 없음 |
| Shizuku 앱 | Fold Transition과 Shizuku 필요 | 시간 제한 없음, Shizuku 재연결 시 복구 시도 |

### 임시 실행 전 사용자에게 설명할 내용

1. 지원 기기와 PC 준비물: Python 3, JDK 17 이상, Android SDK Platform 36,
   Build Tools 36.0.0, Platform Tools(ADB), USB 데이터 연결.
2. 휴대폰에서 개발자 옵션과 USB 디버깅을 켜고, **사용자가 신뢰하는 PC**의
   디버깅 허용 창을 승인해야 한다. USB 경로에는 Wi-Fi 페어링이 필요 없다.
3. APK 설치나 루팅 없이 같은 블러 엔진을 ADB shell 권한으로 실행한다.
   다만 DEX 실행 파일과 잠금 파일은 기기의 `/data/local/tmp`에 만들어진다.
   “설치 없음”을 “기기에 파일을 전혀 쓰지 않음”으로 설명하지 않는다.
4. 기본 600초 후 종료한다. PC의 실행 터미널과 USB 연결을 유지하도록 안내하고,
   연결을 끊은 뒤에도 계속 동작한다고 보장하지 않는다. 재부팅하면 다시 실행해야 한다.
5. 홈·앱·켜진 잠금 화면에서 접고 펼칠 때만 효과를 표시한다.
   꺼진 화면/AOD에서는 표시하지 않는다. 실제 화면을 캡처·저장·업로드하지 않는다.
6. 각도는 주로 0/90/180도만 관측된다. `--early`는 vendor 이벤트 시각으로
   초기 움직임을 추정한다. 연속 각도나 아주 작은 역회전을 정확히 읽는다고 말하지 않는다.
7. 켜진 화면에서 4Hz 진단 폴링을 사용한다. 장시간 배터리 영향은 아직 미검증이다.
8. 종료 명령과 기존 Shizuku 앱과의 충돌 방지 방법을 함께 알려준다.

### 임시 실행 절차

아래 명령은 저장소 루트에서 실행한다. `adb`를 PATH에 두거나 SDK의
`platform-tools/adb` 절대 경로를 사용한다. Python 도구는 `JAVA_HOME`과
`ANDROID_SDK_ROOT`를 읽는다. `local.properties`만으로 이 도구의 SDK/JDK 경로가
설정된다고 안내하지 않는다. Windows 호스트 실행은 아직 검증되지 않았다.

```sh
adb devices -l
adb shell getprop ro.product.model
python3 tools/fold-system.py run --seconds 600 --early
```

- 기기가 여러 대면 각 ADB 명령에 `-s SERIAL`, Python 명령에 `--serial SERIAL`을
  넣는다. 대상 기기를 임의로 고르지 않는다.
- `unauthorized`는 휴대폰의 PC 허용 창을, `offline`은 연결 상태를 확인한다.
- `run`은 빌드·JVM 테스트·DEX 전송 후 실행한다. APK를 빌드하거나 설치할 필요가 없다.
- 지원 모델 제한을 임의로 제거하지 않는다. 비공개 API는 펌웨어에 따라 달라질 수 있다.
- 앱이 실행 중이면 먼저 앱이나 알림에서 **효과 끄기**를 누른다. 두 실행 방식은
  잠금 파일을 공유한다. `Already running`/잠금 오류를 권한 부족으로 오진하지 않는다.
- `--early`를 빼면 공개 각도 센서만 사용하므로 움직임 시작 감지가 늦을 수 있다.
- 임시 실행 시간은 현재 1–3600초를 지원한다. 기본은 600초로 유지하고, 임시 체험을
  몰래 백그라운드 무제한 실행이나 부팅 등록으로 바꾸지 않는다.

다른 터미널에서 조기 종료할 수 있다.

```sh
python3 tools/fold-system.py stop
```

이 명령은 기록된 PID의 명령행을 확인한 뒤 **독립 ADB 시험 프로세스만** 종료한다.
Shizuku 앱 엔진은 앱이나 알림에서 끈다. 단순 Ctrl+C/USB 분리를 종료 확인으로
간주하지 말고, 필요하면 다시 연결해 `stop`과 프로세스 상태를 확인한다.
자동 종료는 프로세스·효과의 종료이며 전송한 DEX 파일 삭제를 뜻하지 않는다.
파일 정리를 요청받으면 모든 엔진이 중지됐는지 확인하고 이 프로젝트가 만든
파일만 대상으로 한다. 실행 중인 DEX를 덮어쓰거나 잠금 파일을 삭제하지 않는다.

### 실행 후 사용자에게 설명할 내용

- 실제 실행한 방식, 대상 모델, 시간 제한, 중지 방법을 알린다.
- 빌드 성공, `READY` 로그, 사용자 실물 확인을 구분한다. `READY`만으로 블러가
  보인다고 단정하지 않는다. `ERROR`/`EARLY_MONITOR_DISABLED`도 확인한다.
- 사용자가 직접 접었다 펼치도록 안내하고 커버·내부·잠금 화면에서의 관찰을 받는다.
- 1.5초 정지 후 약 420ms 동안 해제되는지와 기존 그라디언트를 확인한다.
- 종료 시 원인이 타임아웃인지 명시적 중지인지 오류인지 구분한다. 예외/비정상
  종료 코드를 일괄 정상 처리하지 않는다. 물리 테스트가 없으면 미검증으로 기록한다.

실행 전 안내 예시:

> 앱 설치 없이 먼저 체험할 수 있어요. PC에 USB로 연결하고 디버깅을 허용하면
> 같은 블러 엔진을 10분간 실행합니다. 기기에 임시 실행 파일은 복사되지만
> APK나 Shizuku 설치, 루팅은 필요 없습니다. PC 연결을 유지해주세요.
> 원하면 종료 명령으로 바로 끌 수 있고, 재부팅 후 자동으로 켜지지는 않습니다.

실행 후 안내 예시(실제로 READY를 확인한 경우):

> 앱 없는 ADB 체험을 시작했습니다. 10분 후 자동 종료됩니다.
> 커버와 내부 화면에서 천천히 접었다 펼쳐보고, 1.5초 멈췄을 때 블러가
> 자연스럽게 사라지는지 확인해주세요. 즉시 중지하려면 다른 터미널에서
> `python3 tools/fold-system.py stop`을 실행하면 됩니다.

## 상시 앱 방식에서 추가로 설명할 내용

- 앱을 닫아도 엔진은 유지되지만 Shizuku가 실행되어 있어야 한다.
- PC의 USB ADB 인증과 Shizuku의 무선 페어링은 별개다.
- Shizuku 13.6의 부팅 자동 시작은 무선 페어링, 신뢰하는 Wi-Fi, 관련 설정 권한이
  필요하다. 자세한 절차와 권한의 영향은 README를 따른다.
- 재부팅 후 완전 자동 실행을 실제로 시험하기 전에는 검증 완료라고 말하지 않는다.
  기기를 재부팅하거나 앱을 설치할 필요가 없는 체험에 이런 설정을 요구하지 않는다.

## 변경과 검증

One UI 유지, 잠금 화면 동작, 커버 오른쪽/내부 왼쪽 절반 그라디언트,
1.5초 정지 해제, 중복 실행 방지, 중지 시 레이어 정리를 보존한다.
기존 사용자 변경을 덮어쓰지 않고 작업 범위에 맞는 검증만 수행한다.

- 엔진 변경: `python3 tools/fold-system.py build`로 JVM 테스트와 DEX 빌드를 확인한다.
- 앱 변경: `./gradlew :app:assembleDebug :app:lintDebug`를 확인한다.
- 문서 변경: 코드와 명령 인자·링크를 대조하고 `git diff --check`를 실행한다.
  문서 검증을 위해 기기에 효과를 켜거나 APK를 재설치하지 않는다.
- 민감한 기기 정보, 인증 키, 개인 화면 내용, 로컬 설정, 빌드 결과물을 커밋하지 않는다.
- 공개 문서의 지원 범위와 검증 상태를 구현에 맞게 유지한다.

## USB 분리 검증

상시 앱의 케이블 독립 동작을 보고하기 전에 Shizuku 앱의 무선 시작으로 실행하고,
실제로 USB를 분리한 상태에서 물리 효과가 유지되는지 사용자 확인을 받는다.
USB로 Shizuku를 다시 시작했다면 마지막 실행 경로가 바뀐 사실을 알린다.
프로세스 복구 시험 성공을 케이블 분리 시험 성공으로 대신하지 않는다.
효과가 사라지면 앱, Shizuku 서버, 엔진의 상태를 구분해 확인한다.
서버가 종료된 경우 앱 코드 오류로 단정하거나, 복구 기능만으로 shell 권한까지
다시 얻을 수 있다고 안내하지 않는다.

## README 언어

`README.md`는 영어 기본 문서, `README.ko.md`는 한국어판이다. 사용법·지원 범위·
검증 상태를 바꿀 때 두 문서를 함께 갱신하고, 상단의 언어 전환 링크를 유지한다.
